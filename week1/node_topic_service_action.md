# Node、Topic、Service、Action 区别
1. Node（节点）
ROS2最小可执行程序单元，独立小程序，一个节点只负责一类功能，可收发消息、调用服务。

2. Topic（话题，异步发布订阅）
广播式数据流，发布者持续发消息，多个订阅者异步接收；无返回应答，适合传感器高频数据。

3. Service（服务，同步一问一答）
客户端发送请求，服务端单次返回结果，交互结束立即断开，适合短时查询。

4. Action（动作，长任务带反馈）
用于耗时操作，支持下发目标、实时进度反馈、最终结果、中途取消，如导航、机械臂运动。
